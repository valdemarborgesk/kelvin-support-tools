{
  "id": "api/schemas/workloadstagedsummary",
  "title": "WorkloadStagedSummary",
  "type": "schema",
  "category": "api",
  "content": "# WorkloadStagedSummary\n\n\n## Properties\n\n- **ready** (`boolean`): Staged workload ready to be applied.\n- **app_version** (`string`): Version Number of the Kelvin App used for the Staged Workload.\n- **deployment_type** (`string`): Specifies the method for deploying a workload on the platform.\n\n- `standard`: The default deployment method. The workload is\nautomatically created and started.\n- `staged+instant-apply`: Similar to `standard`, but if the workload\nexists with a different app image, the new image is downloaded before\nstopping the existing workload. This minimizes application downtime.\n- `staged-only`: Prepares the workload by downloading the necessary\nimages, but does not create or start the workload. The workload is only\nstarted upon explicit user request (see `/workloads/apply`).\n- **status** (`object`): Realtime information about the Staged Workload as seen from the Cloud.",
  "summary": "Object with properties: ready, app_version, deployment_type, status",
  "source": "openapi.json",
  "code_examples": [],
  "related": [],
  "tags": [
    "schema",
    "type",
    "api"
  ],
  "metadata": {
    "schema_name": "type.WorkloadStagedSummary",
    "schema_type": "type",
    "schema": {
      "type": "object",
      "properties": {
        "ready": {
          "type": "boolean",
          "description": "Staged workload ready to be applied."
        },
        "app_version": {
          "type": "string",
          "format": "semver",
          "description": "Version Number of the Kelvin App used for the Staged Workload."
        },
        "deployment_type": {
          "type": "string",
          "enum": [
            "standard",
            "staged+instant-apply",
            "staged-only"
          ],
          "description": "Specifies the method for deploying a workload on the platform.\n\n- `standard`: The default deployment method. The workload is\nautomatically created and started.\n- `staged+instant-apply`: Similar to `standard`, but if the workload\nexists with a different app image, the new image is downloaded before\nstopping the existing workload. This minimizes application downtime.\n- `staged-only`: Prepares the workload by downloading the necessary\nimages, but does not create or start the workload. The workload is only\nstarted upon explicit user request (see `/workloads/apply`)."
        },
        "status": {
          "type": "object",
          "description": "Realtime information about the Staged Workload as seen from the Cloud.",
          "properties": {
            "message": {
              "type": "string",
              "description": "Descriptive, human-readable string for `state`."
            },
            "state": {
              "type": "string",
              "enum": [
                "pending_deploy",
                "pending_update",
                "pending_start",
                "pending_stop",
                "pending_apply",
                "deploying",
                "running",
                "stopping",
                "stopped",
                "failed",
                "starting",
                "applying",
                "received",
                "downloading",
                "ready",
                "unreachable",
                "staged"
              ]
            },
            "warnings": {
              "type": "array",
              "description": "All warnings received for any Staged Workload operations.",
              "items": {
                "type": "string"
              }
            }
          }
        }
      }
    },
    "last_updated": "2026-02-04"
  }
}